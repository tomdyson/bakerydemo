def extract_entities(text, salience_threshold=0.15, max_results=50):
    "extract entities from plain text, whose salience exceeds the threshold"
    return ["Wagtail", "Torchbox", "Python", "Django"]